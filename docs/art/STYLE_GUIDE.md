# 아트 스타일 가이드 (M1 확정판)

- **최종 수정일**: 2026-07-27
- **담당**: art-director
- **의존 문서**:
  - `docs\design\GAME_CONCEPT.md` (2026-07-15 디렉터 결정 반영판 — 톤 밝음6:어둠4, 단일 왕국·도시 8개+, 실시간 액션, 주야간)
  - `docs\design\worldview.md` (아스터 왕국 — 권역/도시 일람 6.4, 종족 4장, 마법·균열 7장)
  - `docs\design\systems\combat.md` (타일 단위 수치, 히트스톱 3단, 예고 모션 0.4~0.8초, 몬스터 패밀리 구조, 5-2-1장 잡몹 넉백 체급 경량/표준/중량)
  - `docs\design\systems\m2-monster-spec.md` (뿔토끼=경량 / 들개 마수=표준 / 균열 점액=중량 체급 배정)
  - `docs\art\ux\ux-foundation.md` (해상도 1920x1080, HUD 좌표·글자 크기, 색+형태 이중 신호 원칙)
  - `docs\art\PIPELINE.md` (뷰포트 1280x720 고정 + Camera2D.zoom=4 구조 확정 — 본 개정에서 변경하지 않음)
  - `docs\art\m3-character-art-plan.md` (2026-07-27 — M3 캐릭터 아트 정식화: 손그림 CC0 베이스 소싱 방향·후보, 20×36 변환 절차, 전사·궁수 프레임 목록, 신규 적 7종 규격, D3-A2-2 판정, 4단계 물량 분할)
  - `docs\design\systems\m3-monster-spec.md` (신규 적 3종 유형·실루엣 요구 8-5장, 아종 4종 7장)
  - `docs\design\systems\m3-archer-skills.md` (10-1장 궁수 아트 요구 — 활 draw·조준 스탠스·후방 회피·곡예 사격)
  - `docs\design\systems\m3-warrior-tier2-skills.md` (8장 검투사 아이콘·이펙트 요구, 12장 D3-A2-2)
- **변경 이력**:
  - 2026-07-15: 최초 작성 (픽셀 해상도 기준, 공통 팔레트 Endesga 32 채택, 아웃라인/명암/애니메이션 규칙, 권역별 색조, 밝음6:어둠4 해석, 전투 가독성 색 위계, 제작 파이프라인 규칙)
  - 2026-07-18: 디렉터 지시(G2-2) — 스타듀밸리 비율 정합. 인간형 스프라이트를 32x32(2타일 폭)에서 **16x32(1타일 폭)**로 슬림화, 몬스터 잡몹 체급을 combat.md 5-2-1의 **경량/표준/중량** 용어와 일치시켜 재정의(1-2장), 스타듀밸리 대비 화면 비율 근거표 신설(1-2-1장). 타일 16px·카메라 줌 4배 구조는 스타듀와 이미 동일하므로 변경하지 않음(근거는 1-2-1장)
  - 2026-07-18 디렉터 지시(2차): 해상도 1280x720 → **1920x1080** 전환에 따라 1-1절 "UI 렌더링" 네이티브 해상도 문구·글자 크기 표기를 갱신 (`ux-foundation.md` 5-0절·`PIPELINE.md` 1장과 동기화). 월드 내부 해상도(320x180)·카메라 줌(x4)·타일 규격은 ui-dev 소관 밖이라 본 갱신에서 다루지 않음 — 1-1/1-2-1장의 "화면 가로 20타일" 등 월드 관련 수치는 art-director 후속 확인 필요(결과 보고 참고)
  - 2026-07-18 (producer, M2 게이트 1차 결과 정리): 위에서 남긴 art-director 확인 필요 플래그를 해소 — 1-1절 "게임 월드 내부 해상도"를 320x180→**480x270**(1920x1080의 정수 1/4)으로, 1-2-1절의 720p 기준 "가로 20타일·화면 폭 320px·점유율 5%/10%" 수치를 1080p 기준 "가로 **30타일**·480px·3.3%/6.7%"로 정정. 타일 크기·줌 배율·결론(인간형 프레임 폭=1타일=스타듀와 일치)은 변경 없음
  - **2026-07-27 디렉터 승인: 스톤샤드식 안 1 — 인간형 20×36·명암 4단·몬스터 비례 재조정** (제안서 `docs\art\pixel-ratio-stoneshard-proposal.md` 안 1 채택). 타일 16px·줌 x4·EDG32·combat 타일수학·UI 좌표는 불변. 개정 범위: (1) 인간형 프레임 16x32 → **20x36**(~3.3등신, 1-2절), (2) 잡몹 3종을 인간형과 동일한 절대 증분(+4px/+4px = 폭 +0.25타일)으로 상향 — 뿔토끼 16x16→**20x20**, 들개 마수 32x24→**36x28**, 균열 점액 32x32→**36x36**(1-2절), (3) 캐릭터·몬스터에 한해 명암 **3단 → 4단 램프 허용**(EDG32 내 인접 명도, 팔레트 확장 없음. 타일·UI·아이콘은 3단 유지, 3-2절), (4) 신규 프레임 화면 대비 근거표 신설(1-2-2절)
  - **2026-07-27 (art-director, M3 아트 태스크)**: 계획서 `docs\art\m3-character-art-plan.md` 확정 내용을 반영. 개정 범위: (1) **1-2절에 신규 적 3종 규격 신설** — 숲거미 **28x24**(벌레) / 무법자 **20x36**(인간형 = 플레이어와 동일) / 임프 **24x28**(균열), 그리고 **"프레임 크기는 유형이 정하고 체급은 넉백 수치가 표현한다"** 3원칙(1-2-3절 신설)으로 "프레임 면적 순서 = 넉백 체급 순서" 요구를 M2 3종 내부 기록으로 격하, (2) 1-2-2절 근거표에 신규 적 3종 화면 점유율 행 추가, (3) **3-3절 프레임 예산 개정** — 1직업 기본 90 + **2차 전직 스킬 모션 +24 별도 예산**, 잡몹 51 + **유틸/방어 블록 1개당 6프레임(2프레임 루프 x 3방향)**, 확장 상태 어휘 확정, (4) **6-1-1절 신설 — D3-A2-2 판정(테마 강조색 예외 화이트리스트)**: 분노/격노/처형의 붉은 강조색을 자원 게이지·자기 상태 오라·조건 충족 순간 3범주에서만 허용, `#ff0044`·궤적·투사체·장판은 금지, (5) **6-3절에 인간형 적 3중 신호 의무 신설**(인간형 적은 플레이어와 프레임이 같아 크기로 구분 불가) + 두신 규칙(인간형 3.3 / 임프 2.5), (6) **7-1절 파일명·시트 규약 확정**(상태 어휘·행 배치·아틀라스 리전 매핑), (7) **7-3절 CC0 소싱 규칙을 A/B/C 라이선스 등급제로 개정** — `ASSET_SOURCES.md` 6·9장의 "CC0만 허용 vs 실제 CC-BY 사용" 불일치 해소, (8) 8장 pixel-artist·vfx-artist·gameplay-dev 요청 갱신. 타일·줌·해상도·EDG32·아웃라인·명암 4단 규칙은 불변
  - **2026-07-27 디렉터 승인(G2-2 부분 되돌림 명문화)**: 2026-07-18 G2-2 확정의 "인간형 = 1타일 폭(16px)"을 "**~1.25타일 폭(20px)**"로 조정한다. G2-2가 명시 확정 사항이라 art-director 임의 변경 대상이 아니었으므로 디렉터 승인으로 되돌림을 기록한다. G2-2의 실제 목적("화면이 좁고 캐릭터가 2타일 폭이라 뚱뚱해 보였다"의 해소)은 유지된다 — 20px는 구 32px(2타일)보다 여전히 슬림하며, 뚱뚱함의 원인이던 2타일 폭에서 벗어나 있다. 1-2-1절 근거표의 "프레임 폭만 조정하면 화면 비율이 정해진다"는 논리의 연장선상 조정이다
  - **2026-07-30 (art-director, M3 3-A 검수·판정)**: pixel-artist가 올린 "인간형 프레임 폭 20px 예외" 판정 요청을 **실측으로 처리**하고 3-A 산출물(전사·궁수 180프레임)을 검수했다. 개정 범위: (1) **1-2절 인간형 캔버스 20x36 → 28x36**(전 상태·전 인간형 액터 통일. 상태별 가변 예외는 **기각**), **실체 규격 정정 15x33 → 17~19 x 32~34**(실측값 — 구 "15x33"은 16x32 치비 시절 전제가 남은 오류), 인간형 적(무법자 계열) 행도 28x36으로 동반 개정, (2) **1-2-3절 가드레일 ① 재정의** — 불변 대상은 "프레임 20x36"이 아니라 **"인간형 척도(실체 높이 33px)"**이며 캔버스는 척도가 아니다. **원칙 4(캔버스 여백 산정 규칙) 신설** — 캔버스 = 실체 + 양측 각 4px 이상 스윙 여백, 4px 배수, **인간형 캔버스 상한 28px**(캔버스 팽창 방지), (3) 1-2-2절 근거표를 **실체 기준 점유율**로 재정의(캔버스 확대는 화면 인상을 바꾸지 않음을 수치로 확인 — 캐릭터는 커지지 않는다), (4) **3-2-2절 신설 — 인간형 얼굴 표현 상한**(눈 1px 점 2개 + 헤어 실루엣이 상한, 입·눈썹·표정 금지. 표정은 초상화 64x64와 상태 UI가 담당), (5) **3-3-1절 몬스터 확장 상태 어휘 12종 확정**(ai-dev 구현 키와 정합) + **프레임 제작 대상/폴백 허용 구분표** 신설 — `guard`는 폴백(`idle`)이 "막고 있음"을 전혀 전달하지 못하므로 **제작 필수**, `roar`는 폴백(`attack`)이 위협 신호를 공격으로 오독시키므로 **제작 필수**, (6) **7장 5번 검증에 "프레임 클리핑 0건" 게이트 신설**(잘린 단면은 3-1 아웃라인 폐곡선 규정 위반), 캔버스 규격 검사값 인간형 28x36으로 갱신, (7) 8장 pixel-artist·gameplay-dev 요청 갱신(재생성 **순서** 명시). 타일 16px·줌 x4·해상도·EDG32·아웃라인·명암 4단·프레임 높이 36·발밑 하단 중앙 앵커는 불변

> 이 문서는 pixel-artist(Pillow 절차 생성 + CC0 소싱), vfx-artist, ux-designer, tech-artist, audio-designer(분위기 톤)가 따르는 **비주얼 단일 기준**이다. 모든 수치와 hex 코드는 확정값이며, 변경은 본 문서 개정(변경 이력 추가) 후에만 허용한다.

---

## 1. 픽셀 해상도 기준 (확정)

### 1-1. 핵심 수치

| 항목 | 확정값 | 근거 |
|---|---|---|
| **타일 크기** | **16x16 px** | ① CC0 에셋 생태계(Kenney, OpenGameArt 등)의 최대 규격 ② Pillow 절차 생성 비용 최소 ③ 250시간 분량 = 맵 물량이 많으므로 타일 제작비를 낮게 고정 |
| **게임 월드 내부 해상도** | **480x270 px** (2026-07-18 개정 — 해상도 전환에 맞춰 720p 기준값을 대체) | 1920x1080의 정수 1/4. 화면에 가로 **30타일** x 세로 16.875타일 표시(1-2-1절 근거표 참조) — 실시간 액션의 교전 시야(잡몹 무리 2~5마리 + 회피 공간)에 적정 |
| **카메라 줌 배율** | **x4 (Godot 4: `Camera2D.zoom = Vector2(4, 4)`)** | 480x270 → 1920x1080 정수 스케일. 비정수 줌 금지 (픽셀 왜곡). 줌 배율 자체는 720p 시절과 동일(1-2-1절) |
| **UI 렌더링** | **네이티브 1920x1080** (CanvasLayer, 줌 비적용, 2026-07-18 1280x720에서 상향) | ux-foundation.md의 HUD 좌표·글자 크기(본문 33px/보조 27px)가 네이티브 px 기준이므로, UI는 월드 줌과 분리한다 |
| **월드 1타일의 화면 크기** | 64x64 px (16 x 4배) | 참고치 |

- 텍스처 필터: **Nearest 고정** (프로젝트 설정 `default_texture_filter = Nearest`). 스프라이트의 런타임 회전·비정수 스케일 금지 (투사체 등 회전이 필수인 오브젝트만 예외 허용, vfx-artist 판단).
- 2D 픽셀 스냅 활성화 (`snap_2d_transforms_to_pixel = true`) — 적용은 tech-artist.
- combat.md의 타일 단위 수치와의 정합: 대시 3타일 = 48px(월드 좌표), 잡몹 근접 스윙 1.5타일 = 24px.

### 1-2. 스프라이트 크기 규격 (2026-07-27 개정 — 스톤샤드식 안 1, 인간형·몬스터 프레임 상향)

**개정 원칙 — "모든 인간형·잡몹 프레임을 각 축 +4px(폭 +0.25타일) 상향"**: 인간형이 16x32 → 20x36으로 각 축 +4px 커지므로, 잡몹 3종에도 **동일한 절대 증분(+4/+4)**을 적용한다. 그 결과 모든 액터가 폭 기준 정확히 +0.25타일씩만 커져 **세계·체급·인간형 대비 상대 비율이 그대로 보존**된다(비대해지지 않음, 1-2-2절 검증). 정예 이상(48x48~)·펫·오브젝트·아이콘·초상화는 본 개정 범위 밖으로 규격 불변 — 해당 자산 제작 시점에 별도 검토.

| 대상 | 캔버스(프레임) | 실체(몸) 크기 | 비고 |
|---|---|---|---|
| **플레이어 / 인간형 NPC** | **28x36** (가로 **1.75타일** x 세로 2.25타일) — 2026-07-30 개정, 구 20x36 | **실체 17~19 x 32~34** (**~3.3등신**, 머리 ~10~11px, 성인 실루엣) — 2026-07-30 실측 정정 | **2026-07-30 개정: 캔버스만 20 → 28px, 실체(척도)는 불변.** 구 표기 "실체 약 15x33"은 16x32 치비 시절 전제가 남은 오류였다 — LPC 손그림 인간형을 높이 33px로 맞추면 어깨·엉덩이 폭이 **정지 자세에서도 17~19px**이 되어 20px 캔버스의 여백이 **좌우 0~1px**뿐이었고, 그 결과 walk 전 프레임에서 주먹이 프레임 경계에 잘린 단면으로 남았다(실측: 18시트 중 17시트가 창을 2~16px 초과). 캔버스를 28로 넓혀 **좌우 각 4~5px 스윙 여백**을 확보한다. **캔버스는 척도가 아니다** — 실체가 그대로이므로 화면상 캐릭터 크기·점유율은 변하지 않는다(1-2-2절). 발밑 기준점(원점)은 캔버스 하단 중앙 **불변**, 프레임 높이 36 **불변**, 콜리전·앵커·씬 offset 변경 **불요**(수평 중앙 정렬이므로 `offset.x = 0` 유지). **연혁**: ~~구 규격 16x32(2.5등신 치비)~~ → 2026-07-27 스톤샤드식 상향(20x36, 등신은 스톤샤드 4.5와 스타듀 2.5의 중간 = 우리 톤 절충, 3-2절 명암 4단 허용) → 2026-07-30 캔버스만 28x36. **콜리전은 하단 발밑 기준 유지**(스타듀식 상체 오버랩) — 상체는 콜리전 없이 배경·다른 캐릭터와 시각적으로 겹칠 수 있다 |
| 펫 | 24x24 | 약 12x14 | **변경 없음**(본 개정 범위 밖) — 몸(12x14)이 플레이어 몸(15x33)보다 확실히 작아 위계 유지 |
| **몬스터(잡몹) — 경량 체급** | **20x20** (가로 ~1.25타일 x 세로 1.25타일) | 약 18x15 | 예: **뿔토끼**(`m2-monster-spec.md` 3-1). 구 16x16 → +4/+4. combat.md 5-2-1 "경량"(넉백 저항 ×1.5)과 용어·대상 일치. 폭은 인간형과 같은 1.25타일이나 높이(20 vs 36)·형태(웅크린 소형 야수+뿔)로 실루엣 명확 구분(6-3절). **여전히 3규격 중 최소 프레임**(면적 400 < 들개 1008 < 점액 1296)이라 경량 위계 보존. 패밀리 아종은 팔레트 스왑(combat.md 9-2) |
| **몬스터(잡몹) — 표준 체급** | **36x28** (가로 2.25타일 x 세로 1.75타일) | 약 30x19 | 예: **들개 마수**(`m2-monster-spec.md` 3-2). 구 32x24 → +4/+4. combat.md 5-2-1 "표준"(넉백 저항 ×1.0)과 용어·대상 일치. 4족보행이라 인간형(실체 18x33)보다 폭넓고 낮은 가로형 실루엣 — 실루엣만으로 인간형과 구분(6-3절) |
| **몬스터(잡몹) — 벌레형 소형** (2026-07-27 신설) | **28x24** (가로 1.75타일 x 세로 1.5타일) | 약 26x16 (다리 포함 폭, 도약 시 세로 22) | 예: **숲거미**·그림자 숲거미(`m3-monster-spec.md` 4-1·7-1). 넉백 체급은 **경량 ×1.5**이나 프레임은 경량 규격(20x20)을 쓰지 않는다 — 1-2-3절 원칙 적용. 근거: 8다리를 좌우로 뻗으려면 폭 26px 필요(20px에서는 다리가 몸에 붙어 털뭉치가 된다), 높이 24는 **도약 예고의 웅크림→펴짐**에 필요한 세로 여유 8px. **3규격 중 유일하게 폭>높이인 소형 프레임**이라 실루엣 즉시 구분(6-3절) |
| **몬스터(잡몹) — 인간형** (2026-07-27 신설, 2026-07-30 캔버스 개정) | **28x36** (플레이어와 동일 — 구 20x36) | 실체 17~19 x 32~34 (~3.3등신) | 예: **무법자**·노상강도·밀렵꾼(`m3-monster-spec.md` 4-2·7-2·7-3). **인간형은 유형·진영과 무관하게 플레이어와 같은 캔버스·척도를 쓴다**(1-2-3절 가드레일 ①). 2026-07-30 개정으로 캔버스가 28x36이 되었으나 **플레이어와 동일하다는 성질이 바로 이 규칙의 요점**이므로 3-B 결정(플레이어 정식화 베이스 재사용)은 그대로 유효하다 — 오히려 무법자의 돌진 예고(상체 낮추고 무기 뒤로 = 가로로 큰 자세)가 20px 창에서 잘릴 문제를 사전에 제거한다. 인간형 캔버스는 **세계 척도의 기준선**이며, 플레이어 정식화 베이스를 적에도 재사용해 물량을 절감한다. 넉백 체급은 표준 ×1.0. **플레이어와 크기가 같으므로 6-3절 "인간형 적 3중 신호"가 의무** |
| **몬스터(잡몹) — 균열 소형** (2026-07-27 신설) | **24x28** (가로 1.5타일 x 세로 1.75타일) | 약 20x24 (몸통 높이 ~18 + 뿔 ~4, 꼬리 측면 돌출 포함 폭 20) | 예: **임프**·포효 임프장(`m3-monster-spec.md` 4-3·7-4). 넉백 경량 ×1.5. 높이 28 < 인간형 36 → **체고 차 8px(0.5타일)이 한눈에 읽힌다.** 폭 24는 뿔 2개 + 꼬리 측면 돌출 수용분. **두신 ~2.5(머리 크게)**로 인간형(3.3)과 구분(6-3절) |
| **몬스터(잡몹) — 중량 체급** | **36x36** (가로 2.25타일 x 세로 2.25타일) | 약 30x29 | 예: **균열 점액**(`m2-monster-spec.md` 3-3). 구 32x32 → +4/+4. combat.md 5-2-1 "중량"(넉백 저항 ×0.5, 정예·보스는 넉백 자체 없음)과 용어 일치. **제안서 예시 40x40(2.5타일)은 디렉터 "과하게 키우지 말 것" 지시에 따라 36x36(2.25타일)으로 절제** — +4/+4 통일 규칙으로 2.25타일 상한을 지키면서 3규격 중 최대 면적 유지. 중량 잡몹 전용 규격이며, 정예 기본 규격은 아래 정예 행(48x48)을 사용한다 |
| 정예 상위 (확장용) | 48x48 | 약 32x32 | 중량 잡몹(36x36)보다 뚜렷이 큰 정예 몬스터용. 본 개정 범위 밖 — 규격 불변 |
| 대형 몬스터 (정예 상위) | 64x64 | 약 48x48 | 변경 없음 |
| 보스 | **최대 96x96** | 자유 | 96 초과 금지 — 화면(320x180)의 절반을 넘으면 회피 시야를 가린다 |
| 오브젝트 (상자·문·채집물) | 16x16 또는 16x32 | 타일 그리드 정렬 | 변경 없음 |
| 아이템 아이콘 | **16x16** (도트 원본) | — | UI에서는 x2(32px) 또는 x3(48px)로 정수 확대 표시. 인벤토리/도감/스킬 아이콘 공통. 변경 없음 |
| 초상화 (HUD·대화) | 64x64 | — | HUD 초상(ux 5절 A요소 64x64)과 공유. 대화창은 동일 원본 x2 확대 허용. 변경 없음 |

- **몬스터 체급 용어는 combat.md 5-2-1(넉백 저항 계수)의 "경량/표준/중량"을 그대로 쓴다** — 기존 "소형/중형/대형"은 잡몹 3규격에서 폐기하고, 정예 상위 이상(48x48~)에서만 크기 서술용으로 계속 쓴다. 프레임 크기 상향(2026-07-27)은 넉백 체급 배정을 바꾸지 않는다 — 뿔토끼=경량 / 들개 마수=표준 / 균열 점액=중량은 그대로이며, 프레임이 커져도 체급 순서(경량<표준<중량)와 프레임 면적 순서가 일치해 시각·수치 정합이 유지된다. **단 이 "면적 순서 = 체급 순서" 일치는 M2 3종 내부에서만 유효한 기록이다** — 유형이 늘어난 M3 이후는 1-2-3절 원칙을 따른다.
- **정예 규격의 적용 범위 (2026-07-27 확정)**: 아래 "정예 상위(48x48)"는 **정예 전용 신규 종**에 적용한다. **잡몹에서 파생한 정예 아종(예: 포효 임프장)은 기반종 프레임을 그대로 유지**하며, 프레임 확대·런타임 스케일 확대를 하지 않는다. 근거 — ① 24x28 → 48x48은 픽셀 재작화(신규 종 제작비)가 되어 `combat.md` 9-2 "아종 제작비 = 기본종의 20~30%" 원칙을 위반한다 ② 런타임 정수 2배 확대는 캐릭터 픽셀 밀도가 배경·타일과 어긋나 1-2-2절 "질감 불일치" 리스크를 직접 유발한다 ③ 비정수 스케일은 1-1절에서 금지다. **정예 위상은 vfx 오라 + 그로기 게이지 UI + 무리 구성이 담당한다.**
- **캔버스(프레임)와 척도(실체)의 분리 (2026-07-30 신설 — 본 개정의 핵심 개념)**: 프레임 크기는 **렌더 캔버스**이고, 세계의 크기 위계를 정하는 것은 **실체(몸) 크기**다. 캔버스는 "실체 + 팔·다리·무기가 자세에 따라 몸 밖으로 나가는 분량"이며, 넓혀도 캐릭터가 커지지 않는다(투명 여백이 늘 뿐). 따라서 ① 화면 점유율·크기 위계·"과하게 키우지 말 것" 지시는 **실체 기준으로 판단**하고(1-2-2절), ② **정예 아종 크기 확대 금지 원칙(아래 항)과 본 캔버스 개정은 충돌하지 않는다** — 그 원칙은 실체·런타임 스케일 확대를 금지한 것이고, 캔버스 여백 확보는 실체를 1px도 키우지 않는다. 캔버스와 척도를 혼동하면 "여백을 주려면 캐릭터를 키워야 한다"는 잘못된 결론에 이른다.
- 기존 M2 산출물(`player_warrior_*`, `mob_rabbit_horned_*`, `mob_wolf_feral_*`, `mob_slime_crack_*`)은 구 규격(전부 32x32 캔버스)으로 제작되어 있어 본 개정 규격과 불일치 — pixel-artist가 신규 규격(플레이어 **20x36** → 2026-07-30 개정으로 **28x36**, 뿔토끼 **20x20**, 들개 마수 **36x28**, 균열 점액 **36x36**, 모두 명암 4단 허용)으로 **전 스프라이트를 재제작**해야 한다(8장 참고).

### 1-2-1. 스타듀밸리 대비 화면 비율 근거표 (재검토 방지용)

> **2026-07-27 갱신 주석**: 아래 표는 2026-07-18 G2-2 당시 인간형 프레임 폭을 32→16(1타일)으로 슬림화한 결정의 근거다. 2026-07-27 스톤샤드식 개정으로 인간형 폭이 **16 → 20(1.25타일)**로 조정됐으므로, 이 표의 "인간형 프레임 폭 = 1타일" 결론은 폭에 한해 **1-2-2절로 대체**된다. 단 이 표의 핵심 논리("화면 폭 점유율은 줌·해상도가 아니라 프레임 폭의 타일 환산값에만 좌우된다")는 여전히 유효하며, 20px 조정도 그 논리의 연장선이다(변경 이력 2026-07-27 G2-2 부분 되돌림 참조). 아래 표 본문은 당시 판단 근거로 원형 보존한다.

이 결정이 "줌을 낮추자"는 이전 논의를 대체하는 이유를 수치로 남긴다. 화면 폭 점유율은 타일 크기·카메라 줌이 아니라 **스프라이트 프레임 폭의 타일 환산값**에만 좌우된다 — 타일·줌이 스타듀와 이미 동일했으므로, 문제는 항상 스프라이트 쪽에 있었다.

이 표는 최초 G2-2 검증(2026-07-18 1차 개정) 당시 720p(1280x720) 기준으로 작성됐다. 같은 날 해상도가 1920x1080으로 전환(94a681d)되며 월드 내부 해상도가 320x180→480x270으로 바뀌었으므로, 아래 수치는 **1080p 기준으로 재계산**했다(2026-07-18 2차 개정). 타일 크기·줌 배율이 그대로이므로 결론(인간형 프레임 폭 = 1타일 = 스타듀와 일치)은 변하지 않는다 — 화면이 더 넓어져 표시 타일 수와 점유율 %가 달라졌을 뿐이다.

| 항목 | 스타듀밸리(1080p 기준) | 본 프로젝트 (개정 전, 32px 프레임) | 본 프로젝트 (개정 후, 16px 프레임 — 현재 확정) | 판정 |
|---|---|---|---|---|
| 타일 크기 | 16x16 | 16x16 | 16x16 | 동일 — 변경 없음 |
| 카메라 배율 | 4배(정수) | 4배(`Camera2D.zoom=4`) | 4배 | 동일 — 변경 없음 |
| 화면 표시 타일 수(가로) | 약 **30타일** | 30타일 (월드 실효 가시 영역 480px ÷ 16px) | 30타일 | 동일 — 변경 없음 (1080p 전환에 따라 720p 시절의 20타일에서 갱신) |
| 인간형 프레임 폭 | 16px = **1타일** | 32px = 2타일 | 16px = **1타일** | **불일치 → 일치로 수정.** 이번 개정의 실제 대상 |
| 인간형 프레임 높이 | 32px = 2타일 | 32px = 2타일 | 32px = 2타일 | 형식상 유지(높이는 원래도 스타듀와 동일) |
| 화면 폭 대비 캐릭터 폭 점유율 | 16/480 = **3.3%** | 32/480 = 6.7% | 16/480 = **3.3%** | 타일·줌이 같으므로 폭 점유율은 "프레임 폭의 타일 수"만 바꾸면 그대로 일치한다(1080p 기준 % 자체는 720p 시절 5%/10%에서 갱신되었을 뿐 결론은 동일) |

- **결론**: 화면이 좁아 보이거나 캐릭터가 커 보였던 원인은 줌·해상도가 아니라 인간형 스프라이트가 가로 2타일(뚱뚱함)로 제작되어 있었기 때문이다. 타일 16px·줌 4배 구조를 그대로 둔 채 인간형 프레임 폭만 32→16(1타일)로 슬림화하면 화면 폭 점유율이 스타듀밸리와 정확히 일치한다. 따라서 "카메라 줌을 2배로 축소하자"는 이전 논의는 **잘못된 진단에 대한 처방**이었고, 이번 개정(스프라이트 슬림화)이 이를 대체한다.
- 몬스터(4족보행 등)는 인간형과 실루엣으로 구분되어야 하므로 이 1타일 폭 규칙을 그대로 따르지 않는다 — 표준 체급(들개 마수)의 32x24처럼 종별 형태에 맞는 폭을 허용한다(1-2절).
- 이 표는 향후 "화면 비율이 이상하다"는 재검토 요청이 들어왔을 때, 줌 구조부터 의심하지 말고 스프라이트 비율을 먼저 확인하도록 남겨두는 근거다.

### 1-2-2. 신규 프레임 화면 대비 근거표 (2026-07-27 스톤샤드식 개정)

내부 해상도 480x270(가로 30타일 x 세로 16.875타일), 타일 16px, 줌 x4 기준. 프레임 크기만 바뀌었고 타일·줌·해상도는 불변이므로, 아래는 신규 프레임 4종의 타일 대비 폭/높이·화면 점유율만 재계산한 것이다.

> **2026-07-30 개정 주석 — 점유율은 실체 기준으로 읽는다**: 아래 표는 최초 작성 시 "프레임 폭 = 캐릭터 폭"이라는 근사 아래 캔버스 크기로 점유율을 계산했다. 2026-07-30 실측으로 그 근사가 인간형에서 깨졌으므로(캔버스 28, 실체 17~19), 인간형 행은 **실체 기준 점유율**을 병기했다. 화면 인상을 결정하는 것은 실체다 — **캔버스 20 → 28 개정으로 캐릭터가 커지지 않는다**는 사실이 이 표의 핵심 확인 사항이다(1-2절 "캔버스와 척도의 분리").

| 대상 | 프레임(px) | 폭(타일) | 높이(타일) | 화면 폭 점유율(÷480) | 화면 높이 점유율(÷270) | 구 규격 폭 점유율(참고) |
|---|---|---:|---:|---:|---:|---:|
| **인간형(플레이어/NPC)** — 2026-07-30 개정 | 캔버스 28x36 / **실체 18x33** | 캔버스 1.75 / **실체 1.13** | 2.25 | **실체 기준 3.75%** (캔버스 기준 5.83%) | 실체 기준 12.2% | 16px일 때 3.33%, 20px 캔버스일 때 4.17% |
| **경량(뿔토끼)** | 20x20 | 1.25 | 1.25 | 4.17% | 7.4% | 16px일 때 3.33% |
| **표준(들개 마수)** | 36x28 | 2.25 | 1.75 | 7.5% | 10.4% | 32px일 때 6.67% |
| **중량(균열 점액)** | 36x36 | 2.25 | 2.25 | 7.5% | 13.3% | 32px일 때 6.67% |
| **벌레형 소형(숲거미)** — 2026-07-27 신설 | 28x24 | 1.75 | 1.5 | 5.83% | 8.9% | (신규 — 해당 없음) |
| **인간형 적(무법자)** — 2026-07-27 신설, 2026-07-30 개정 | 캔버스 28x36 / 실체 18x33 | 캔버스 1.75 / 실체 1.13 | 2.25 | 실체 기준 3.75% | 실체 기준 12.2% | (신규 — 인간형과 동일) |
| **균열 소형(임프)** — 2026-07-27 신설 | 24x28 | 1.5 | 1.75 | 5.0% | 10.4% | (신규 — 해당 없음) |

- **2026-07-30 위계 검증 재확인(캔버스 개정 후)**: 인간형 캔버스가 28x36(면적 1008 = 들개 마수와 동일)이 되었으므로 **아래 "면적 순서" 검증은 캔버스로 하면 무의미해진다** — 1-2절 신설 원칙대로 **위계는 실체로만 판단**한다. 실체 면적: 뿔토끼 270 < 숲거미 416 ≈ 임프 480 < 인간형 594 < 들개 마수 570~ / 점액 870. 인간형(18x33)은 여전히 들개(30x19)·점액(30x29)보다 **좁고 높은** 세로형이라 실루엣 구분(6-3절)은 그대로 성립하며, 캔버스가 같아져도 화면에서 보이는 것은 실체뿐이므로 체감 위계에 변화가 없다.
- **신규 3종 위계 검증(2026-07-27, 캔버스 기준 — 상기 재확인으로 대체됨)**: 면적은 숲거미 672 = 임프 672 < 무법자 720 < 들개 마수 1008 < 균열 점액 1296이며, 최소는 여전히 뿔토끼 400이다. 숲거미와 임프는 면적이 같지만 **종횡비가 정반대**(가로형 1.75x1.5 ↔ 세로형 1.5x1.75)라 실루엣 혼동이 없다. 세 종 모두 잡몹 상한 36x36(2.25타일) 이내이고 변이 4px 배수라 1-2-3절 원칙을 충족한다.
- **비대해지지 않음 검증**: 모든 액터의 폭 점유율이 구 규격 대비 정확히 **+0.83%p**(= +4px ÷ 480)만 증가했다. 즉 인간형·경량·표준·중량 **모두 폭 기준 +0.25타일씩 동일하게** 커졌으므로, 상호 상대 크기와 세계(타일) 대비 비율은 개정 전과 동일하게 유지된다. 제안서 2장 ★의 "캐릭터를 세계 대비 더 작게(B)"도, 반대로 "비대하게(과다 확대)"도 아닌, **밀도·비율만 스톤샤드 방향으로 당기고 스케일 관계는 보존**하는 안 1(A) 해석의 수치적 확인이다.
- 스톤샤드(캐릭터 높이 점유율 6.7%)와의 대비: 우리 인간형은 13.3%로 여전히 스톤샤드보다 크게(줌인) 보인다 — 이는 실시간 액션 교전의 캐릭터 인지 가독성을 위해 의도한 것이며, 개정으로 바뀌지 않는다(제안서 2장 ★ (B) 비채택).
- **질감 불일치 유의(제안서 안 1 리스크)**: 16px 청키 타일 위에 고밀도(4단·36px) 캐릭터·몬스터를 얹으면 "배경은 거칠고 캐릭터만 매끈"한 위화감이 생길 수 있다. M2 수직 슬라이스 실제 화면에서 눈으로 확인하고, 과하면 캐릭터 명암을 3.5단 수준(4단 램프를 부분 영역에만 절제 적용)으로 낮춘다 — 튜닝은 M2 재플레이 게이트에서 디렉터 확인.

### 1-2-3. 프레임 크기 결정 원칙 (2026-07-27 신설 — 유형이 늘어난 M3 이후 공통 규칙)

M2 3종(뿔토끼·들개 마수·균열 점액)은 "프레임 면적 순서 = 넉백 체급 순서"가 우연히 일치했으나, **유형(벌레·인간형·균열)이 늘어나면 이 일치를 전 종에 강제할 수 없다.** 숲거미와 임프는 둘 다 넉백 **경량**이지만, 경량 규격 20x20에 8다리 방사형 벌레와 뿔·꼬리 달린 악마를 모두 담으면 4단 램프(3-2-1절)가 뭉개지고 실루엣 구분(6-3절)이 무너진다. 따라서 아래 3원칙으로 대체한다.

1. **체급(경량/표준/중량)은 넉백 수치(`combat.md` 5-2-1)로만 표현한다.** 프레임 면적과 체급의 순서 일치 요구는 폐기한다(M2 3종 내부 기록으로만 보존).
2. **프레임 크기는 유형별 실루엣 요구가 결정하되, 두 가드레일을 절대 지킨다** — ① **인간형의 척도는 불변**(진영·유형 무관. 플레이어·NPC·인간형 적 공통, 세계 척도의 기준선). **2026-07-30 재정의: 불변 대상은 "프레임 20x36"이 아니라 "실체 높이 33px·~3.3등신"이다.** 캔버스는 척도가 아니므로(1-2절) 여백 요구에 따라 개정될 수 있고, 실제로 2026-07-30에 28x36으로 개정됐다. **인간형끼리는 캔버스도 서로 같아야 한다**(플레이어 = NPC = 인간형 적 = 28x36) — 이것이 3-B의 베이스 재사용과 6-3-1절 3중 신호 의무가 성립하는 전제다. ② **잡몹 실체 상한 = 폭 30px·높이 30px 이내, 캔버스 상한 36x36(2.25타일)** (디렉터 "과하게 키우지 말 것" 지시 — 실체 기준으로 읽는다).
3. **모든 프레임 변은 4px 배수(16/20/24/28/32/36)로만 고른다** — 2026-07-27 개정의 "+4/+4 균등 증분" 규율 계승. 신규 규격이 임의 수치로 늘어나는 것을 막는다.
4. **캔버스 여백 산정 규칙 (2026-07-30 신설 — 20px 사고의 재발 방지)**: 캔버스는 **실체 + 양측 각 4px 이상의 스윙 여백**으로 산정한다(4px = 실측으로 walk·hit·dodge·cast의 팔·발 돌출을 전량 흡수하는 값). 캔버스를 실체와 같은 크기로 잡으면 **정지 자세는 통과하지만 모든 동작 상태가 잘린다** — 이것이 20x36 규격이 규격 검사를 통과하면서도 잘린 프레임을 낳은 원인이다.
   - **인간형 캔버스 상한 = 28px (엄수)**. 28로도 담기지 않는 자세(실측 잔여: 전사 `attack` 5px·`attack2` 7px·`charge` 5px, 궁수 `death` 8px·`rollshot` 7px·`cast` 8px)는 **캔버스를 더 넓혀 해결하지 않는다** — **팔 스윙 호를 프레임 안으로 재작화(clamp)**하거나 **원본 자세를 더 좁은 자세로 교체**한다(무기에 이미 적용한 방식의 팔·다리 확장. `m3-character-art-plan.md` 14장). 상태마다 캔버스를 키우면 캔버스 팽창이 끝나지 않고, 7-1절 아틀라스 규약의 "시트당 W 단일" 성질도 잃는다.
   - **신규 규격을 정할 때는 실체 목표를 먼저 정하고 캔버스를 역산한다**(예: 숲거미 실체 폭 24 → 캔버스 24+4+4 = 32 → 4px 배수 충족. 현행 28x24는 여백 2px/측이라 **도약·거미줄 자세의 클리핑 리스크가 남아 있다** — 7장 5번 클리핑 검증에서 걸러 실체 폭을 24 이하로 조정한다).

- 위 원칙에 따라 신규 규격을 추가할 때는 반드시 **1-2절 표에 행을 추가하고 크기 근거를 1행 이상 기록**한다(근거 없는 규격 신설 금지).

### 1-3. 한글 폰트 (픽셀 스타일 유지)

- UI 본문: **갈무리(Galmuri) 11** / 보조 라벨: **갈무리 9** — 한글 완성형 지원 픽셀 폰트, SIL OFL(무료·재배포 가능).
- ux-foundation.md의 최소 크기(본문 16px, 보조 13px)를 만족하도록 정수 배수로만 확대. 안티앨리어싱 끄기.

---

## 2. 공통 팔레트 (확정): Endesga 32

**마스터 팔레트 = Endesga 32 (EDG32), 총 32색.** 무료 공개 팔레트로, 채도가 높고 명랑한 기본색과 깊은 어둠색을 모두 갖춰 "밝음 6 : 어둠 4" 톤에 부합한다. **모든 스프라이트·타일·이펙트·UI는 아래 32색만 사용한다** (예외: 반투명 오버레이의 알파값, 3-4절의 라이팅 모듈레이트).

| # | HEX | 이름(관용) | 주 용도 |
|---|---|---|---|
| 1 | `#be4a2f` | 벽돌 적갈 | 지붕·벽돌, 남부 테라코타 |
| 2 | `#d77643` | 주황 갈색 | 목재 밝은 면, 피부 그림자(짙은 피부) |
| 3 | `#ead4aa` | 모래 크림 | 회벽·모래, 중부 석재 밝은 면 |
| 4 | `#e4a672` | 살구 | 피부 기본, 흙길 밝은 면 |
| 5 | `#b86f50` | 황토 | 동부 변경 대지 기본색 |
| 6 | `#733e39` | 적갈 어둠 | 목재·대지 그림자 |
| 7 | `#3e2731` | 흑자주 | 균열 지대 바닥, 어두운 실내 |
| 8 | `#a22633` | 검붉음 | 피·상처, 문벌파 문장색 |
| 9 | `#e43b44` | 선홍 | **적(敵) 식별 기본색**, HP 바, 디버프 테두리 |
| 10 | `#f77622` | 불꽃 주황 | 용광로·횃불, 폭발 이펙트 중심 |
| 11 | `#feae34` | 황금 | **S등급**, 명성·공훈 연출, 경험치 바 |
| 12 | `#fee761` | 밝은 노랑 | 광원 하이라이트, 루팅 반짝임, NPC 표식(미니맵 노란 점) |
| 13 | `#63c74d` | 신록 | 회복 이펙트, **B등급**, 초원 기본 |
| 14 | `#3e8948` | 초록 | 수림 기본, 초원 그림자 |
| 15 | `#265c42` | 심록 | 실비엔 대수림 어둠 |
| 16 | `#193c3e` | 흑록 | 심야 숲, 심해 |
| 17 | `#124e89` | 남색 | 바다 깊은 면, 왕권파 문장색 |
| 18 | `#0099db` | 청색 | **아군/플레이어 식별 기본색**, MP 바, **A등급** |
| 19 | `#2ce8f5` | 시안 | 플레이어 이펙트 하이라이트, 마나·숨결 발광 |
| 20 | `#ffffff` | 흰색 | 피격 플래시, 텍스트 기본, 최상위 하이라이트 |
| 21 | `#c0cbdc` | 밝은 회청 | UI 비활성 텍스트, 눈(雪)·은속 밝은 면 |
| 22 | `#8b9bb4` | 회청 | **C등급**, 석재·강철 기본, 북부 산맥 기본 |
| 23 | `#5a6988` | 짙은 회청 | 강철 그림자, 야간 원경 |
| 24 | `#3a4466` | 남회색 | UI 패널 밝은 면, 야경 건물 |
| 25 | `#262b44` | 짙은 남색 | UI 패널 기본, 밤하늘 |
| 26 | `#181425` | 최암(最暗) | **아웃라인 표준색**, UI 최하층 배경. ※ 순수 `#000000` 사용 금지 |
| 27 | `#ff0044` | 경고 적 | **적 공격 예고(텔레그래프) 전용** — 다른 용도 사용 절대 금지 (6장) |
| 28 | `#68386c` | 암자색 | 균열·오염 숨결, 저주 디버프 |
| 29 | `#b55088` | 자주 | 균열 발광 하이라이트, 히든/신비 연출 |
| 30 | `#f6757a` | 분홍 | 치유 성천, 축제 장식, 꽃 |
| 31 | `#e8b796` | 밝은 살구 | 피부 하이라이트, 커튼·천 |
| 32 | `#c28569` | 황갈 | 동부 변경 대지 밝은 면, 가죽 장비 |

### 2-1. 역할 고정색 (시스템 연동 — 변경 금지)

| 시스템 | 색 | 근거 |
|---|---|---|
| 아이템 등급 S / A / B / C | `#feae34` / `#0099db` / `#63c74d` / `#8b9bb4` | ux 접근성 원칙에 따라 항상 등급 문자(S/A/B/C) 병기 |
| HP / MP / EXP | `#e43b44` / `#0099db` / `#feae34` | ux-foundation 5절 HUD와 일치 |
| 버프 / 디버프 아이콘 테두리 | `#63c74d` / `#e43b44` | ux 접근성: 색+테두리 형태 이중 신호 |
| 미니맵 점: 적 / NPC / 퀘스트 목표 | `#e43b44` / `#fee761` / `#feae34`(별 모양) | ux 5절 B요소 |
| UI 패널 배경 / 텍스트 | `#262b44`(불투명 90%) / `#ffffff`, 보조 `#c0cbdc` | 흰색 텍스트 대비 4.5:1 이상 확보 |

### 2-2. 팔레트 준수 규칙 (pixel-artist 필수)

1. Pillow 절차 생성물은 **32색 외의 색을 1픽셀도 포함하지 않는다** (안티앨리어싱 발생 금지). 최종 저장 전 팔레트 검증 스크립트로 확인.
2. CC0 소싱 에셋은 **반드시 EDG32로 양자화(quantize)** 후 사용 — `Image.quantize(palette=EDG32)`로 자동 변환 가능. 변환 후 뭉개진 부분만 수동 보정.
3. 지역별 색조(4장)는 새 색 추가가 아니라 **32색 중 부분집합(서브 팔레트) 선택**으로 구현한다.

---

## 3. 아웃라인 · 명암 · 애니메이션 규칙

### 3-1. 아웃라인 (선택적 아웃라인 방식)

| 대상 | 규칙 |
|---|---|
| 캐릭터·몬스터·펫·상호작용 오브젝트 | **1px 외곽선 필수, 색은 `#181425`** — 배경에서 즉시 분리(전투 가독성의 1차 장치) |
| 지형 타일·배경 오브젝트(나무·바위 등 비상호작용) | **외곽선 없음** — 색 명도 차로만 구분. 전경(캐릭터)과 배경의 시각 계층을 아웃라인 유무로 나눈다 |
| 아이템 아이콘 | 1px `#181425` 외곽선 필수 (어두운 UI 패널 위 가독성) |
| 보스 | 외곽선 `#181425` 기본. 그로기 상태에서만 외곽선을 `#fee761`로 교체 (딜 타임 신호) |

- 내부 선(옷 주름 등)은 외곽선보다 밝은 색(해당 재질 램프의 그림자색)을 쓴다. 캐릭터 내부에 `#181425` 남용 금지.

### 3-2. 명암

| 항목 | 규칙 |
|---|---|
| 광원 | **위쪽 고정** (탑다운 기준 화면 상단, 약간 왼쪽). 모든 스프라이트·타일 공통 |
| 재질당 명암 단계 | 대상별 상한 이원화(2026-07-27 개정): **타일·배경 오브젝트·UI·아이템 아이콘 = 3단 램프 고정**(기본색 + 그림자 1 + 하이라이트 1), **캐릭터·몬스터(플레이어·NPC·잡몹·펫·보스) = 최대 4단 램프 허용**(아래 4단 규칙). 3단 초과 금지 대상이 명확히 나뉘므로, 프레임이 작은 타일에서 뭉개짐·제작비 증가를 막으면서(3단), 오래 응시하는 캐릭터에만 스톤샤드식 볼륨감(4단)을 부여한다 |
| 디더링 | 원칙 금지. 예외: 하늘·물 등 4타일 이상 대면적 그라데이션에만 2색 체커 디더 허용 |
| 그림자(드롭 섀도) | 캐릭터·몬스터 발밑에 **타원형 반투명 그림자(`#181425`, 알파 40%)** 1장 공통 사용 — 점프·넉백 시 높이 인지용. 방향성 캐스트 섀도는 만들지 않는다 |
| 채도 규칙 | 그림자는 "어둡게+약간 차갑게"(남색 방향), 하이라이트는 "밝게+약간 따뜻하게". 검정을 섞은 탁한 그림자 금지 |

#### 3-2-1. 캐릭터·몬스터 4단 램프 규칙 (2026-07-27 신설, 스톤샤드식 볼륨감)

프레임 상향(인간형 실체 18x33 등)으로 음영을 넣을 픽셀 공간이 늘어난 캐릭터·몬스터에 한해, 재질당 명도 단계를 **4단까지** 허용한다. 스톤샤드의 다단계 볼륨 셰이딩을 EDG32 제약 안에서 근사하는 장치다.

1. **4단 구성**: 명도 순으로 **하이라이트 1 + 기본색 + 그림자 2**, 또는 **하이라이트 2 + 기본색 + 그림자 1** — 재질당 총 4개 명도. 3단(기존)도 그대로 허용하며, 4단은 **상한이지 의무가 아니다**. 작은 프레임(뿔토끼 20x20 등)이나 좁은 재질 영역에서 4단이 뭉개지면 3단으로 절제한다(pixel-artist 판단).
2. **팔레트 확장 없음**: 4단은 반드시 **EDG32 32색 내의 인접 명도**만 조합해 만든다. 새 색을 추가하거나 중간색을 섞지 않는다(2장·2-2절 팔레트 준수 규칙 그대로). 램프 예시:
   - 피부: `#e8b796`(하이라이트) → `#e4a672`(기본) → `#d77643`(그림자1) → `#b86f50`(그림자2)
   - 강철·회청: `#c0cbdc` → `#8b9bb4` → `#5a6988` → `#3a4466`
   - 초록(야수 가죽 등): `#63c74d` → `#3e8948` → `#265c42` → `#193c3e`
   - 균열 점액: `#b55088`(발광 하이라이트) → `#68386c`(기본) → `#3e2731`(그림자1) → `#262b44`(그림자2)
3. **채도 방향 유지**(3-2 채도 규칙): 4단이 되어도 그림자 2단은 "어둡게+약간 차갑게(남색 방향)", 하이라이트는 "밝게+약간 따뜻하게"를 지킨다. 검정을 섞은 탁한 그림자 금지 — 위 램프처럼 명도가 낮아질수록 한색 계열 색상으로 넘어가게 고른다.
4. **아웃라인 불변**: 4단 램프와 무관하게 외곽선은 3-1절 그대로 1px `#181425`. 내부 선은 램프의 그림자색을 쓰고 `#181425` 남용 금지(3-1 원칙 유지).
5. **적용 대상 명확화**: 4단 허용 = 캐릭터·몬스터·펫·보스. **타일·배경 오브젝트·아이템 아이콘·UI 프레임은 3단 고정**(3-2 표). 이펙트는 6-2절 규칙(EDG32·알파만 사용)을 따르며 볼륨 셰이딩 대상이 아니다.

#### 3-2-2. 인간형 얼굴 표현 상한 (2026-07-30 신설 — art-director 판정, 재시도 방지용 규정)

3-A 검수에서 "얼굴이 눈 점 2개뿐이고 표정이 없다"는 보고가 올라왔다. **판정: 이 해상도의 정상 상한으로 확정하며, 얼굴 디테일을 늘리는 방향의 재작업을 금지한다.**

- **근거(수치)**: 인간형 실체 폭 17~19px에서 머리 폭은 10~11px이고, 아웃라인 1px(3-1절)과 헤어 실루엣을 빼면 **얼굴에 남는 유효 면적은 약 6~7px 폭 × 5~6px 높이**다. 여기에 눈·눈썹·입을 모두 넣으면 인접 픽셀이 서로 붙어 **표정이 아니라 얼룩**이 된다(0.70배 축소 + 4단 양자화에서 원본 눈이 소실된 것도 같은 원인).
- **확정 상한**: **눈 = 1px 점 2개**(눈 사이 간격 2~3px) + **헤어 실루엣**. 입·눈썹·코·홍채·시선 방향·표정 변화는 **만들지 않는다**.
- **앞/뒤 신호 겸용 규정**: **후면(행 2)에는 눈을 찍지 않는다** — 눈의 유무가 정면/후면 판별 신호를 겸한다(3-A에서 채택된 처리를 규정으로 승격).
- **표정·감정의 담당은 다른 층위다**: 대화·컷 연출의 표정은 **초상화 64x64**(1-2절)가, 전투 중 상태(격노·피격·저주 등)는 **상태 오라·플래시·HUD 아이콘**(6장)이 담당한다. 캐릭터 스프라이트에 표정을 요구하는 기획은 초상화 또는 UI 요구로 번역한다.
- **얼굴이 "빈 살색 덩어리"로 보이는 경우의 정상 처방**: 눈을 늘리는 것이 아니라 **헤어·투구 실루엣의 대비를 올리고 머리와 몸의 명도 단계를 벌린다**(4단 램프, 3-2-1절).

### 3-3. 애니메이션 프레임 수 기준

방향: 스프라이트는 **4방향**(하/상/좌/우, 좌우는 미러 허용 → 실제 제작 3방향). 이동은 8방향이지만 대각선은 가까운 좌/우 스프라이트를 사용한다.

| 동작 | 프레임 수 | 재생 속도 | 비고 |
|---|---|---|---|
| idle | **4** | 6 fps | 호흡 2px 이내 상하 움직임 |
| walk/run | **6** (CC0 소싱 몬스터는 4 허용) | 10 fps | 플레이어는 6 고정 |
| attack (플레이어) | **4** | 12 fps | 선딜 1 + 타격 1 + 후딜 2. 무기 콤보(2~3타)는 같은 4프레임 규격을 타수만큼 제작 |
| attack (몬스터) | **5** | 예고 구간 가변 | **예고 2프레임(0.4~0.8초 유지, combat.md 4장)** + 발동 2 + 회수 1. 예고 프레임은 실루엣이 명확히 변해야 한다(치켜들기, 웅크리기 등) |
| hit(피격) | 1 | 0.25초 표시 | 흰색 플래시 셰이더(6장)와 병용 |
| death | 4 | 8 fps | 마지막 프레임에서 페이드아웃 |
| 스킬 시전(캐스팅) | 2 (루프) | 8 fps | 차징·설치형 공용 |

- **플레이어 1직업당 기본 프레임 예산: 90프레임 이내** (3방향 x 위 규격 + 회피 3 + 콤보 추가분). 초과 기획은 art-director 리뷰 필수.
- **2차 전직 스킬 모션 추가 예산: +24프레임 이내 (별도 예산, 2026-07-27 신설)** — 90프레임 예산은 M1 시점에 **1차 전직 기준**으로 산정된 값이라 2차 전직(승계·신규 4스킬)의 모션이 포함돼 있지 않다. 상한 24를 두어 무제한 팽창을 막는다. 실제 배정 예: 전사 검투사 = 검투 선풍 4(1행 공용) + 처형 일격 12 = 16, 난입 강타·혈투의 함성은 재사용 0(`m3-character-art-plan.md` 5-2절).
- **재사용 판정 기준 (2026-07-27 신설)**: 신규 스킬 모션의 제작 여부는 **"실루엣이 이미 존재하는 모션과 같은가"**로 판정한다. 같으면 기존 시트 재사용(예: 돌진 = 대시 시트), 다르면 제작(예: 처형 일격의 머리 위 치켜듦). 자기 중심 360° 회전처럼 **한 애니메이션 안에서 전 방향을 지나가는 모션은 1행(방향 무관) 제작**으로 3방향분을 절감한다.
- 잡몹 1종당 기본 예산: 3방향 x (idle 4 + walk 4 + attack 5 + death 4) = **51프레임 이내**. 아종은 팔레트 스왑이므로 0.
- **잡몹 유틸/방어 블록 추가 예산: 블록 1개당 6프레임 이내 (2프레임 루프 x 3방향, 2026-07-27 신설)** — `combat.md` 9-2의 무피해 유틸/방어 블록(거미줄 조준·가드·포효 등)은 데미지 예고가 아니지만 플레이어가 읽어야 하는 상태이므로 최소 표현이 필요하다. 잡몹 1종 실질 상한 = 51 + 6 = **57프레임**(유틸 블록 1개 기준). 예: 숲거미 `_web` 6 / 무법자 `_guard` 6 / 임프 `_roar` 6.
  - **초단시간 상태의 이펙트 대체 허용**: 예고·후딜이 0.3초 이하인 상태(순간이동 소멸·등장 등)는 **`_idle` 프레임 + 알파 페이드 셰이더 + 이펙트**로 대체하고 스프라이트 신규 프레임을 만들지 않는다. 단 **상반된 두 상태(사라지는 중 ↔ 나타나는 중)가 시각적으로 구분되어야 한다** — vfx-artist 검수 항목.
- 보스: 프레임 규격 자유이나 동작당 6프레임 이내, 총 120프레임 이내 권장.

#### 3-3-1. 확장 상태 어휘 (2026-07-27 확정 — 새 어휘 추가는 본 문서 개정 후에만)

파일명·애니메이션 키에 쓰는 상태 이름을 고정한다. 임의 신설을 막아 시트·코드·문서의 이름이 갈라지는 것을 방지한다.

| 대상 | 필수 상태 | 확정된 확장 상태 |
|---|---|---|
| 플레이어 | `idle` `walk` `attack` `hit` `death` | `attack2`(콤보 2타) `dodge`(회피) `charge`(차지 홀드 루프) `cast`(버프·시전 루프) `spin`(360° 회전) `execute`(피니셔) `aim`(조준 스탠스 루프) `rollshot`(구르며 사격) |
| 몬스터 | `idle` `walk` `attack` `death` | `web`(거미줄 조준) `guard`(가드) `roar`(포효) / **2026-07-30 추가 확정**: `crouch`(도약 예고 웅크림) `leap`(공중) `land`(착지) `charge_telegraph`(돌진 예고) `charge`(돌진 이동) `aim`(석궁 조준) `hit`(피격) `vanish`(소멸) `appear`(등장) |

##### 3-3-1-1. 몬스터 확장 상태의 제작 대상 / 폴백 허용 구분 (2026-07-30 신설 — art-director 판정)

ai-dev가 M3 신규 행동 블록 6종을 구현하면서 위 9개 어휘를 코드에 도입했고, `_play_animation_or(상태, 폴백)`이 **해당 상태 프레임이 있으면 자동 승격 / 없으면 폴백 재생**한다. 어휘를 문서에 확정하는 동시에, **어느 상태가 프레임 제작 필수이고 어느 상태가 폴백으로 충분한지**를 아트 측에서 판정한다. 기준은 하나다 — **폴백이 플레이어에게 거짓 정보를 주는가.**

| 상태 | 코드 폴백 | 판정 | 근거 |
|---|---|---|---|
| `guard` (무법자) | `idle` | **제작 필수 (6프레임)** | 폴백이 정지 자세라 "막고 있음"이 **전혀 전달되지 않는다.** 정면 ±60° 방향 의존 메커니즘인데 신호가 없으면 플레이어는 "왜 데미지가 안 들어가지"만 겪는다 — 난이도 체감을 직접 왜곡 |
| `roar` (임프) | `attack` | **제작 필수 (6프레임)** | 폴백이 공격 모션이라 **무피해 강화 신호를 공격 예고로 오독**시킨다. 없는 것보다 나쁜 신호(회피를 낭비하게 만든다) |
| `web` (숲거미) | `attack` | 제작 권장 (6프레임) | 거미줄은 실제로 둔화 피해를 주므로 폴백(공격 모션)이 **거짓은 아니다.** 다만 예고 구분이 안 돼 회피 타이밍 학습이 늦어진다 → 우선순위 3번 |
| `charge_telegraph` (무법자) | `attack` | **폴백 허용 (0프레임)** | `attack` 시트가 이미 **예고 2프레임(상체 낮추고 무기 뒤로)**을 포함한다(3-3절 몬스터 attack 규격) — 폴백이 정확히 의도한 예고를 재생한다. 별도 시트는 중복 제작 |
| `charge` (무법자) | `walk` | 폴백 허용 (0프레임) | 돌진 이동 구간. 이동 모션 폴백이 자연스럽고, 속도감은 vfx 잔상이 담당 |
| `crouch`·`leap`·`land` (숲거미) | `attack`·`walk`·`attack` | 폴백 허용 (0프레임) | 도약 3단계는 `attack` 5프레임 규격(예고 2 = 웅크림 + 발동 2 = 공중·착지 + 회수 1)에 **이미 전부 들어 있다.** 별도 어휘는 코드 상태 이름일 뿐 신규 프레임 요구가 아니다 |
| `aim` (밀렵꾼) | `attack` | 폴백 허용 (0프레임) | 밀렵꾼 `_attack` 시트 자체를 석궁 조준 사격으로 교체하므로(7-4절, 15프레임) 폴백이 곧 조준 모션이다 |
| `hit` (몬스터 공통) | `idle` | **프레임 만들지 않음 (0프레임)** | 몬스터 피격 피드백은 **흰색 플래시 셰이더 2프레임**(6-1절 2순위)이 담당한다는 기존 규정 유지. 3-3절 프레임 표에도 몬스터 `hit` 행이 없다 — 잡몹 51프레임 예산을 지키는 전제다 |
| `vanish`·`appear` (임프) | `idle` | **프레임 만들지 않음 (0프레임)** | 3-3절 "초단시간 상태의 이펙트 대체" 판정 유지(예고·후딜 0.3초). 소멸/등장의 시각 구분은 vfx-artist 검수 항목 |

- **결과 예산**: 무법자 = 51 + `guard` 6 = **57**, 임프 = 51 + `roar` 6 = **57**, 숲거미 = 51 + `web` 6 = **57**. 3-3절 "잡몹 실질 상한 57프레임"과 정확히 일치하며 **예산 증액이 필요 없다** — 신규 어휘 9종이 늘었어도 프레임은 늘지 않는다는 것이 본 판정의 요점이다.
- **새 상태 어휘를 코드에 도입할 때는 위 표에 행을 추가**한다(제작 필수/폴백 허용 판정 없이 어휘만 늘리면 프레임 예산이 근거 없이 팽창한다).

---

## 4. 지역별 색조 방향 (worldview.md 6.4 연동)

각 권역은 **주조색 4~6개 + 포인트색 1~2개**의 EDG32 서브 팔레트로 정의한다. 타일셋·배경 오브젝트는 해당 서브 팔레트를 우선 사용하고, 캐릭터·UI·이펙트의 역할 고정색(2-1, 6장)은 전 지역 공통 유지 — **"지역이 바뀌어도 전투 언어는 바뀌지 않는다."**

| 권역 (도시) | 무드 한 줄 | 주조색 | 포인트색 |
|---|---|---|---|
| **중부 왕령** (브란텔·그란시아·아르셀) | 정돈된 권위 — 밝은 석재와 황금 들판, 그러나 차갑게 정렬된 질서 | `#ead4aa` `#e4a672` `#8b9bb4` `#63c74d` `#3e8948` | 황금 `#feae34`(왕권·문장원), 남색 `#124e89`(왕권파 깃발) |
| **남부 해안** (살레노·오란세) | 돈과 볕의 활기 — 왕국에서 가장 밝은 팔레트 | `#0099db` `#2ce8f5` `#ead4aa` `#be4a2f` `#e4a672` | 치유 분홍 `#f6757a`(오란세 성천), 노랑 `#fee761` |
| **북부 강철산맥** (두르간·하프나·카른두름) | 차가운 바위와 뜨거운 용광로의 대비 | `#8b9bb4` `#5a6988` `#3a4466` `#c0cbdc`(눈·강철) | 용광로 `#f77622`(두르간·카른두름의 유일한 온기), 검붉음 `#a22633` |
| **서부 실비엔 대수림** (미스란) | 깊고 오래된 숲 — 인간의 색이 옅어지는 땅 | `#265c42` `#3e8948` `#63c74d` `#193c3e` `#733e39` | 숨결 발광 `#2ce8f5`, 신비 자주 `#b55088`(엘프 감응) |
| **동부 변경** (발크렌·노베라 — **시작 지역·주 무대**) | 폐허 위의 재건 — 황토와 잿빛에 새싹의 초록이 섞이는 상승기의 색 | `#b86f50` `#c28569` `#733e39` `#e4a672` `#63c74d`(재건·새싹) | 개척 깃발 노랑 `#fee761` |
| **동부 균열 지대** (필드·던전) | 오염된 숨결 — 게임에서 가장 어두운 팔레트 | `#3e2731` `#262b44` `#68386c` `#193c3e` | 균열 발광 `#b55088` (※ `#ff0044`는 예고 전용이므로 환경 발광에 사용 금지) |

- 도시별 세부: 그란시아는 중부 팔레트에서 `#feae34` 비중 상향(가장 화려·배타적), 아르셀은 `#124e89`·`#8b9bb4` 상향(학술·호반), 발크렌은 동부 팔레트에 `#8b9bb4` 성벽 + 대란의 흉터(`#3e2731` 균열 자국) 혼합, 노베라는 동부 팔레트 중 가장 밝게(신도시 목재 `#d77643` + 깃발 다색).
- 종족 시각 코드: 엘프 관련 요소 = 심록+시안 감응 발광, 드워프 관련 요소 = 회청 강철+룬 각인 주황(`#f77622`), 인간 문벌 = 황금+검붉음, 왕권 = 남색+황금. 명명 규칙(worldview 6.2)처럼 색으로도 계급·종족이 읽히게 한다.

---

## 5. "밝음 6 : 어둠 4"의 비주얼 해석

원칙 (worldview 2.1 "낮은 밝지만, 그늘은 깊다"의 화면 번역):

1. **비율은 콘텐츠 단위로 적용한다** — 화면 전체에 회색 필터를 씌우는 방식이 아니라, "밝은 장소 6 : 어두운 장소·순간 4"로 공간과 연출을 배분한다. 도시·마을·평시 필드는 항상 밝게, 균열 지대·폐허·특정 스토리 연출만 어둡게.
2. **평시(밝음 6)**: 고채도·고명도 서브 팔레트, 하늘은 밝고 광원은 풍부하게. 일상 콘텐츠(수집, 펫, 영지, 축제)는 EDG32의 난색·원색 계열을 아끼지 않는다.
3. **어두운 순간(어둠 4)**: 다음 3가지 장치만 사용하고, 그 이상 어둡게 만들지 않는다.
   - **팔레트 축소**: 해당 씬의 타일·배경을 어둠 계열 서브 팔레트(`#262b44` `#3a4466` `#3e2731` `#68386c` 중심)로 교체. 순수 검정 화면 금지 — 가장 어두운 값은 `#181425`.
   - **라이팅 모듈레이트**: CanvasModulate를 한색(寒色)으로 — 진지한 대화·회상 `#b8c0d4`(채도 저하+한색화), 균열 지대 `#8a86a8`.
   - **광원의 고립**: 어두운 씬일수록 광원(횃불 `#f77622`, 숨결 `#2ce8f5`)을 작고 적게 두어 "그늘의 깊이"를 만든다. 어둠 속의 밝은 점 = "그럼에도 나아가는 사람들"의 시각화.
4. **금지**: 데저추레이션(흑백) 풀스크린 필터, 비네트 남용, 공포 연출 수준의 암전. 어둠 4는 "침울"이지 "호러"가 아니다.

### 5-1. 주야간 표현 (게임 내 시간 30분=1일 연동)

| 시간대 | CanvasModulate 기준값 | 비고 |
|---|---|---|
| 낮 | `#ffffff` (무보정) | 기본 상태 |
| 황혼/새벽 | `#e0a878` / `#aab0d0` | 각 게임 시간 1시간 분량 동안 선형 보간 |
| 밤 | `#6d7ab5` | 야간 몬스터 강화(x1.2)의 시각 신호. 이 값에서도 적 예고색 `#ff0044`와 HUD 가독성이 유지되어야 한다(UI는 CanvasLayer라 모듈레이트 비적용) |

- 위 값은 시작 기준값이며 M2 수직 슬라이스에서 tech-artist와 함께 튜닝한다. 밤에도 잡몹 실루엣(1px 아웃라인)이 배경과 구분되는지가 튜닝 합격 기준.

---

## 6. 실시간 전투 가독성 원칙 (vfx-artist 준수 기준)

실시간 액션 + 무리 전투(2~5마리) + 펫 동반이므로, **색의 소유권**을 고정한다. 아래 위계는 지역·시간대와 무관하게 절대 불변.

### 6-1. 색 위계 (우선순위 순)

| 순위 | 소유자 | 색 | 규칙 |
|---|---|---|---|
| 1 | **적 공격 예고 (텔레그래프)** | `#ff0044` (+ 외곽 `#e43b44`) | **게임 전체에서 이 색은 예고에만 쓴다.** 장판 예고 = `#ff0044` 테두리 + 알파 35% 채움, 발동 직전 2회 점멸. 환경·의상·UI 사용 절대 금지 |
| 2 | **피격 피드백** | `#ffffff` | 피격 플래시(화이트 셰이더) — 몬스터 피격 시 2프레임(약 0.06초, 히트스톱 '중'과 동기). 플레이어 피격 시 화면 가장자리 `#e43b44` 알파 25% 0.3초 |
| 3 | **플레이어·아군 이펙트** | `#0099db` `#2ce8f5` + 하이라이트 `#ffffff` | 플레이어 스킬은 청·시안 계열 통일. 직업 차별화는 색이 아니라 **형태**(궤적·파형)로 한다 |
| 4 | **회복·버프** | `#63c74d` (+`#fee761` 반짝임) | 펫의 버프 스킬 포함 |
| 5 | **적 이펙트 (예고 이후 실공격)** | `#e43b44` `#a22633` | 균열·저주 계열 적만 `#68386c` `#b55088` 허용 |
| 6 | **중립 정보** | `#fee761`(루팅), `#feae34`(명성·희귀 연출) | |

#### 6-1-1. 테마 강조색 예외 (2026-07-27 신설 — art-director 판정 D3-A2-2)

`m3-warrior-tier2-skills.md` 12장이 art-director에 위임한 D3-A2-2("분노/격노/처형의 붉은 강조색을 6장 청·시안 통일 원칙의 예외로 허용할지")에 대한 **확정 판정**이다. 상세 근거는 `m3-character-art-plan.md` 8장.

**판정: 조건부 허용 (화이트리스트 방식).** 아래 3범주에 한정해 붉은 강조색을 허용하며, `#ff0044`는 어떤 경우에도 제외한다.

**판정 근거 요약**: 6-1이 지키려는 것은 ① 적 예고(`#ff0044`)와의 혼동 방지 ② 아군/적 색 소유권 고정이다. "청·시안 통일"은 그 **수단**이다. 6-1이 붉은색을 배정한 소유자(적 예고 1순위·적 실공격 5순위)는 모두 **월드 공간에 놓여 회피 판단에 쓰이는 신호**인데, 자원 게이지는 HUD(CanvasLayer)·상태 오라는 캐릭터 본체 밀착·조건 강조는 1~2프레임 섬광이라 "회피할 위협"으로 오독될 위치·형태가 아니다. 또한 UI 자원 색은 2-1절(역할 고정색, HP 바 = `#e43b44`)의 관할이며 6-1의 적용 대상이 아니다.

| # | 허용 범주 | 색 | 세부 규칙 |
|---:|---|---|---|
| **A** | **자원 게이지(HUD)** — 분노 바·아이콘 | 바 기본 `#a22633` / 충전분 `#e43b44` / 만땅 발광 테두리 `#feae34` | **바 기본색을 `#a22633`(검붉음)으로 HP 바(`#e43b44`)보다 어둡게** 하고, **바 높이·아이콘 형태로 이중 신호**를 준다(ux 접근성 — 색만으로 구분 금지). HP 바와 나란히 놓이므로 필수 |
| **B** | **자기 상태 오라** — 격노 등 플레이어 상태 | 기본 `#a22633` + 하이라이트 `#e43b44` | **캐릭터 실루엣 외곽 2px 이내** 밀착, **알파 ≤ 50%**. 아웃라인 `#181425`을 덮지 않는다. 파티클 ≤ 16 |
| **C** | **조건 충족 순간 강조** — 처형 보너스 발동 등 | `#e43b44` + `#feae34` | **1~2프레임 섬광만.** 지속 표시 금지. 대상 위 표시라면 예고 장판과 겹치지 않는 **대상 실루엣 내부**로 한정 |

**금지 범위 (예외 없음)**

| 금지 항목 | 근거 |
|---|---|
| **`#ff0044` 사용(전 범주)** | 6-1 1순위 적 예고 전용. "게임 전체에서 이 색은 예고에만 쓴다"는 절대 규정 — 본 판정으로도 바뀌지 않는다 |
| **스킬 궤적·참격선·투사체·장판을 붉게** | 월드 공간의 회피 판단 신호와 같은 층위. 검투 선풍 궤적·난입 강타 잔상·처형 일격 참격선은 **전부 청·시안 유지** |
| 화면 폭 64px 초과 붉은 광역 연출 | 6-2 화면 점유 규격. 상태 오라를 전화면 붉은 필터로 확장 금지 |
| 회복·버프 연출의 붉은색화 | 흡혈이 테마상 붉더라도 `#63c74d`(6-1 4순위) 유지 — 회복 신호의 색 소유권이 우선 |

- **선례 규정**: 이후 붉은 테마 직업(격투가 등)이 테마색 예외를 요청하면 **위 3범주(자원 게이지 / 자기 상태 오라 / 조건 충족 순간 강조)에 들어가는지로만 판정**한다. **궤적·판정 형태·장판은 직업·테마와 무관하게 항상 청·시안이다**(6-1 3순위 불변, 직업 차별화는 색이 아니라 형태).

### 6-2. 이펙트 제작 규칙

| 항목 | 규칙 |
|---|---|
| 지속 시간 | 기본 공격 히트 이펙트 ≤ 0.2초 / 일반 스킬 ≤ 0.5초 / 궁극기 ≤ 1.2초. 화면에 잔상이 쌓이지 않게 한다 |
| 파티클 수 | GPUParticles2D 이미터당: 일반 스킬 ≤ 32, 궁극기 ≤ 64 |
| 화면 점유 | 일반 스킬 이펙트는 내부 해상도(320x180) 기준 폭 64px 이내. 궁극기만 전화면 연출 허용(0.5초 이내) |
| 알파 | 이펙트 스프라이트도 EDG32 색만 사용, 반투명은 알파값으로만 (중간색 생성 금지) |
| 가림 금지 | 이펙트가 적 예고(1순위)를 덮지 않도록, 예고 장판은 항상 이펙트보다 위 레이어에 그린다 |
| 히트스톱 연동 | 약/중/강 3단(combat.md 5-3)에 대응해 이펙트 크기 소/중/대 3규격을 짝지어 제작 — `HitFeedback` 모듈의 이펙트 슬롯 규격 |

### 6-3. 실루엣 원칙

- 같은 화면에 서는 플레이어 / 펫 / 잡몹 / NPC는 **크기 규격(1-2)과 아웃라인(3-1)만으로도 구분**되어야 한다. 색은 보조 수단 (ux 접근성: 색만으로 상태 구분 금지 원칙의 전투판).
- 몬스터 패밀리의 아종(팔레트 스왑)이라도 위험도가 다르면 실루엣 요소 1개(뿔, 갈기 등)를 추가한다 — "색만 다른 몹" 체감 방지(combat.md 9-2)와 색맹 대응을 겸함.

#### 6-3-1. 인간형 적 3중 신호 의무 (2026-07-27 신설)

**인간형 적(무법자·노상강도·밀렵꾼 등)은 프레임·척도가 플레이어와 동일하므로(28x36, 실체 18x33 — 2026-07-30 개정) 크기로 구분할 수 없다**(1-2·1-2-3절 가드레일 ①의 필연적 결과). 위 6-3절 "크기 규격과 아웃라인만으로 구분" 원칙의 **명시적 예외**로서, 아래 3중 신호를 **모두** 의무화한다 — 하나라도 빠지면 규격 위반이다.

| 신호 | 요구 |
|---|---|
| ① **실루엣 요소** | 머리 윗선에 **후드**(뾰족한 실루엣 + 목덜미로 늘어지는 천 2~3px) 등 플레이어에 없는 형태 요소 필수. 무기도 플레이어 대검(긴 직선)·활(비대칭 곡선)과 다른 형태(**단검·손도끼**처럼 짧고 굽은 것)를 쓴다 |
| ② **적 액센트색** | 어깨·허리에 **`#e43b44`(2장 "적 식별 기본색") 1~2px** 액센트 의무. **`#ff0044`는 절대 금지**(6-1 1순위 예고 전용) |
| ③ **적 UI** | 몬스터 HP 바 표시(적 공용 UI) — 색+형태 이중 신호(ux 접근성) |

**두신 규칙 (2026-07-27 신설)**: 같은 세로형 실루엣끼리는 **머리 비율로 구분**한다 — **인간형(플레이어·인간형 적) = ~3.3두신**, **소형 악마·균열 개체(임프 계열) = ~2.5두신(머리 크게)**. 이 수치를 지키지 않으면 임프가 "작은 사람"으로 보여 유형 식별이 실패한다. 가로형끼리(숲거미 ↔ 들개 마수)는 **다리 개수·몸통 높이**로 구분한다(거미 8다리 방사형·몸통 낮게 밀착 / 들개 4다리·목과 꼬리로 전후 축이 뚜렷) + 팔레트 분리(거미 = 심록·흑록, 들개 = 회청·황토).

---

## 7. 제작 파이프라인 규칙 (pixel-artist · tech-artist)

1. **원본 관리**: 스프라이트 시트는 PNG, 프레임은 캔버스 규격(1-2) 격자 정렬. 파일명 snake_case: 플레이어 `player_<직업>_<상태>.png`, 몬스터 `mob_<이름>_<상태>.png` (예: `player_warrior_walk.png`, `mob_outlaw_thug_attack.png`). 상태 어휘는 3-3-1절 확정 목록만 사용한다. **아종은 기반종과 공통 접두를 쓴다** — `mob_outlaw_thug` / `mob_outlaw_highwayman` / `mob_outlaw_poacher`처럼 파일명만으로 아종 관계가 읽히게 한다(2026-07-27 개정 — 구 "`_색이름` 접미" 규칙 대체).
   - **시트 레이아웃 확정**: 가로 = 프레임(열), 세로 = 방향(행). **행 0 = front(하) / 행 1 = side(측) / 행 2 = back(상)**, 좌우는 `flip_h`(실제 제작 3방향). **시트 크기 = `(프레임 수 x W) x (3 x H)`** (예: 궁수 walk = (6x28) x (3x36) = 168x108 — 2026-07-30 인간형 W 개정 반영). 프레임 `(열 c, 행 r)`의 아틀라스 리전 = **`Rect2(c*W, r*H, W, H)`**. 예외는 방향 무관 1행 시트(3-3 재사용 판정 기준)뿐이며, 그 사유를 반드시 문서에 기록한다.
   - **W는 "시트당 단일 상수"다 (2026-07-30 명문화)**: 같은 액터 유형의 **전 상태가 같은 W를 쓴다.** 상태별로 W가 달라지면 리전 계산은 시트 단위라 형식상 성립하더라도, ① 검증 스크립트의 기대값이 상태별 분기가 되고 ② 좌우 flip·정렬의 기준이 상태마다 달라져 회귀 검출이 어려워지며 ③ 인간형 적이 플레이어 베이스를 재사용하는 구조(6-3-1절)가 상태별로 깨진다. **2026-07-30 판정: "공격·시전 상태만 폭 예외" 제안은 이 성질을 잃는 대가가 이득보다 커서 기각**하고, 인간형 전 상태를 28로 통일했다(1-2절).
   - **W 변경 시 씬 배선 영향 (2026-07-30 실측)**: `AnimatedSprite2D`의 `offset`은 수직 보정만 쓰고(`offset = Vector2(0, -18)` = -H/2) **수평은 0(텍스처 중앙 정렬)**이므로, **몸 중심이 프레임 중앙에 있는 한 W가 바뀌어도 offset·콜리전·앵커·y-정렬을 고칠 필요가 없다.** 따라서 W 개정의 실제 비용은 **시트 재생성 + `SpriteFrames` 리소스 재생성**뿐이다. 단 **몸 중심을 프레임 중앙에서 벗어나게 그리는 것은 금지** — 그 순간 이 성질이 무너진다.
   - **발밑 기준점 = 캔버스 하단 중앙** 고정. **전 상태·전 프레임에서 발밑 y좌표가 동일**해야 한다(걸을 때 캐릭터가 위아래로 튀지 않도록) — 검증 항목.
   - 소싱 원본은 `godot\assets\tools\_raw_src\`에만 두고 **저장소에 커밋하지 않는다**(`ASSET_SOURCES.md` 7장 재현 절차 유지).
2. **절차 생성(Pillow) 우선 대상**: 지형 타일셋, 아이템 아이콘, 오브젝트, UI 프레임(9-slice), 이펙트 시트 — 규칙적·대량 생산에 유리한 것. **반대로 관절이 있는 몸의 자세·모션(캐릭터·몬스터 애니메이션)은 절차 생성의 구조적 약점**이다(무게 이동·예비동작·잔여동작이 좌표 규칙으로 표현되지 않는다) — 이것이 M2 전사 스프라이트 지적의 원인이었다(`m3-character-art-plan.md` 1장).
3. **CC0 소싱 우선 대상**: 캐릭터·몬스터 애니메이션 — 프레임 애니메이션은 **손그림 원본에서 자세·타이밍을 가져오는 것**이 절차 생성보다 품질이 높다. 출처·라이선스는 `godot\assets\CREDITS.md`(최소 크레딧) + `docs\art\ASSET_SOURCES.md`(전체 기록) 양쪽에 기록한다.
   - **"축소가 아니라 재작화" 원칙 (2026-07-27 신설)**: 원본 캔버스(64x64·100x100 등)를 20x36으로 단순 리샘플하면 무기·얼굴·손이 뭉개진다. **캔버스 크기가 아니라 실체(몸) 알파 bbox로 축소율 `r`을 산출**하고(목표는 **실체 높이 33px** — 폭은 결과값이다. 2026-07-30 정정), `r ≥ 0.8`이면 NEAREST 축소 + 수동 정리 / `0.5 ≤ r < 0.8`이면 축소본을 초벌로만 쓰고 재작화 / `r < 0.5`면 원본을 참고 레이어로만 깔고 최종 캔버스(인간형 28x36)에서 처음부터 그린다. 절차 상세는 `m3-character-art-plan.md` 4장.
   - **처리 순서 엄수**: **축소·재작화 → EDG32 재색상 → 아웃라인 보정.** 재색상을 먼저 하면 축소 보간으로 32색 밖 색이 다시 생긴다(M2에서 확인된 함정).
   - 공용 유틸 `sprite_source_common.recolor_ramp4`(휘도 5밴드 = 아웃라인 + 4단 램프)를 재사용한다.

4. **임포트**: Nearest 필터, mipmap 끄기. 공통 임포트 프리셋은 tech-artist가 구성.
5. **검증**: 커밋 전 팔레트 검증(32색 외 색 검출) + 캔버스 규격 검사 스크립트 통과 — pixel-artist가 검증 스크립트를 `tools\`에 작성해 공용화할 것. **캔버스 규격 검사값은 1-2절 표 갱신 시 함께 갱신**한다(현행 대상: 플레이어·인간형·인간형 적 **28x36**(2026-07-30 개정) / 뿔토끼 20x20 / 숲거미 28x24 / 임프 24x28 / 들개 마수 36x28 / 균열 점액 36x36).
   - **5-1. 프레임 클리핑 검증 게이트 (2026-07-30 신설 — 필수)**: 생성 스크립트는 프레임별로 **실체가 캔버스 밖으로 나간 픽셀 수**를 측정해 보고하고, **위반 0건이어야 통과**다(`gen_player_lpc.py --report` 방식을 전 캐릭터·몬스터 생성 스크립트로 확대). 근거 — 잘려나간 단면에는 아웃라인이 없어 **3-1절 "1px 폐곡선 외곽선 필수"를 위반**하며, 배경과의 분리(6-3절)도 그 지점에서 끊긴다. 20x36 시절 이 검사가 없어 **18시트 중 17시트가 클리핑을 안고 규격 검사를 통과**했다.
   - **판정 기준**: 클리핑이 잡히면 **① 자세·스윙 호를 프레임 안으로 재작화 → ② 실체 목표 크기 하향 → ③ (인간형 28px 상한 이내에서만) 캔버스 조정** 순으로 처리한다. 캔버스 확대를 1순위 처방으로 쓰지 않는다(1-2-3절 원칙 4).

### 7-1-1. 소싱 라이선스 등급 (2026-07-27 개정 — art-director 판정)

구 7-3절은 "CC0만 허용"이었으나 M2 실적은 CC-BY 3종이었다(`ASSET_SOURCES.md` 6·9장이 이 불일치를 올려둔 상태). 본 프로젝트가 `CLAUDE.md`에 명시된 **비배포 개인 프로젝트**임을 근거로 등급제로 정리한다(방향 변경이 아니라 현실 정합).

| 등급 | 라이선스 | 허용 범위 | 기록 의무 |
|---|---|---|---|
| **A** | CC0 / Public Domain | **픽셀 직접 편입 무제한** | `ASSET_SOURCES.md` 출처 1행(표기 의무는 없으나 재현성 위해 기록) |
| **B** | CC-BY, OGA-BY, CC-BY-SA, GPL | **픽셀 직접 편입 허용** | **`CREDITS.md` + `ASSET_SOURCES.md` 양쪽 필수** — 저작자·라이선스·URL·(택1 허용 소스는) 선택한 라이선스 명기. CC-BY-SA/GPL은 파생물 동일 라이선스 의무가 있어 **비배포 전제에서만** 사용하고 그 전제를 함께 기록 |
| **C** | 재배포 금지 커스텀 라이선스 | **픽셀 편입 금지.** 포즈·프레임 수·타이밍 **참고만** | `ASSET_SOURCES.md`에 "참고 전용" 명시 |
| **금지** | 라이선스 표기 없음 / 불명 | 사용 금지 | — |

- **A등급 우선 원칙**: 동등한 후보가 있으면 항상 A를 고른다. B를 고를 때는 "왜 A로 안 되는가"를 `ASSET_SOURCES.md`에 1행 기록한다.
- **가장 교체하기 어려운 자산에 가장 무거운 라이선스를 두지 않는다** — 플레이어 캐릭터(교체 비용 최대)는 A등급 고정, 적 의상 같은 보조 자산은 B등급 허용.
- **배포 검토 시 재검토 의무**: B·C등급 자산은 배포를 검토하는 시점에 전면 재검토 대상이다(`PROJECT_STATUS.md` 백로그 등재 대상).
## 8. 타 담당자 확인 요청

| 대상 | 요청 |
|---|---|
| pixel-artist | 본 가이드 기준으로 M2 수직 슬라이스 분량(동부 변경 타일셋 1종, 전사 1직업, 몬스터 3종) 제작 착수 가능. 팔레트 검증 스크립트 공용화(7-5). **2026-07-27 스톤샤드식 개정 신규 규격으로 캐릭터·몬스터 전 스프라이트 재제작**: `player_warrior_*` → **20x36**(~3.3등신, 4단 명암), `mob_rabbit_horned_*` → **20x20**(경량), `mob_wolf_feral_*` → **36x28**(표준), `mob_slime_crack_*` → **36x36**(중량). 4종 모두 명암 4단 램프 허용(3-2-1절), 팔레트는 EDG32 불변, 아웃라인 1px `#181425` 불변. 타일셋·아이콘·오브젝트·UI는 재제작 대상 아님(3단·규격 불변). 검증 스크립트의 캔버스 규격 검사값을 신규 4종 규격으로 갱신할 것 |
| pixel-artist (M3 추가, 2026-07-27) | **캐릭터 아트 정식화(C-6·B1) 및 신규 적(C-9)**: 상세 프레임 목록·파일명·시트 크기·소싱 후보·변환 절차는 `m3-character-art-plan.md`가 마스터다. 요약 — ① 정식화 대상은 **전사·궁수 2직업뿐**(각 90프레임 기본 + 전사 검투사 16), 손그림 CC0 베이스 1순위는 Foozle "Legend - Main Character"(CC0, 검 3종 + 활 2종) ② **최선행 작업 = 원본 실체 bbox 측정**(7-3 "축소가 아니라 재작화" 원칙, r 산출) ③ 신규 적 3종은 **정식화 대상이 아니며** 규격만 준수(숲거미 28x24 / 무법자 **28x36**(2026-07-30 개정) / 임프 24x28, 각 57프레임) ④ 아종 4종은 팔레트 스왑·소품 수준(밀렵꾼 `_attack` 15프레임만 신규) ⑤ 7-1-1 라이선스 등급별 기록 의무 이행(`ASSET_SOURCES.md` + B등급은 `CREDITS.md` 동시) |
| **pixel-artist (2026-07-30 3-A 검수 결과)** | **① 인간형 캔버스 28x36 재생성** — `lpc_common.py`의 `FRAME_W` 상수 20 → **28** 1곳 변경 후 `gen_player_lpc.py`(180프레임) → `gen_player_spriteframes.py`(`.tres` 2개) 재실행. **실행 시점은 gameplay-dev의 `player.tscn` 배선 커밋 완료 후**(8장 gameplay-dev 행). 파일명·애니메이션 키·프레임 수는 불변이므로 배선은 그대로 유효하다 ② **클리핑 잔여 6상태를 재작화로 해소**(전사 `attack`·`attack2`·`charge`, 궁수 `death`·`rollshot`·`cast`) — 캔버스를 28 이상으로 넓히는 처방 금지(1-2-3절 원칙 4) ③ **대검 재작화** — 현 검은 LPC `arming`(한손검) 기반이라 `jobs.md` 전사 정체성("대검, 느리지만 무거운 한 방")과 불일치. 칼날 길이 ≥ 20px·폭 2px·십자 가드 3px·자루 2px로 상향(28px 창이 이를 가능하게 한다) ④ **어깨 견갑 2~3px 직접 작화 + 금속/천 명도 대비 강화**(가슴판 하이라이트 `#c0cbdc` / 천 `#3a4466`~`#262b44`) — 중장 인상은 판금 추가가 아니라 어깨 폭·금속 대비로 만든다 ⑤ **활이 머리·얼굴 픽셀을 덮지 않게** 손 위치를 어깨선 아래로 내리거나 활 중심을 몸 바깥으로 3px 이상 밀어낸다(현행 궁수 `cast`·`rollshot`·`attack` 정면에서 활이 얼굴을 가린다) ⑥ 7장 5-1 클리핑 검증 게이트를 몬스터 생성 스크립트에도 적용 ⑦ 얼굴 디테일 추가 작업은 **금지**(3-2-2절) |
| vfx-artist | 6장 색 위계·이펙트 규칙 준수. `HitFeedback` 3단 규격 이펙트부터 제작. **(M3 추가) 6-1-1절 테마 강조색 화이트리스트 준수** — 붉은색은 A 자원 게이지 / B 자기 상태 오라 / C 조건 충족 순간 3범주만, 궤적·투사체·장판은 청·시안, `#ff0044` 절대 금지. **임프 순간이동은 스프라이트 프레임 0**(3-3 초단시간 상태 이펙트 대체)이므로 소멸/등장 이펙트가 상태 신호를 전담하며 **두 상태가 시각적으로 구분되어야 한다**(검수 항목) |
| ux-designer / ui-dev | 2-1 역할 고정색을 HUD 구현 색상값으로 사용. 갈무리 폰트(1-3) 채택 확인 |
| tech-artist | x4 정수 스케일 구성(월드 SubViewport 또는 Camera2D zoom=4 + UI CanvasLayer 분리), Nearest·픽셀 스냅 프로젝트 설정, 주야간 CanvasModulate(5-1) 구현 |
| audio-designer | 4장 권역 무드(중부=정돈/남부=활기/북부=한랭+금속/서부=신비/동부=재건과 긴장)와 5장 밝음6:어둠4 해석을 BGM 톤 기준으로 공유 |
| level-designer | 타일 16x16, 화면 30x16.875타일 시야를 맵 설계 기준으로 사용 (2026-07-18 1080p 전환 반영, 1-1·1-2-1절) |
| ui-dev (M3 추가, 2026-07-27) | **분노 게이지 바 색 확정값(6-1-1 A)**: 바 기본 `#a22633` / 충전 `#e43b44` / 만땅 테두리 `#feae34`. HP 바(`#e43b44`)와 나란히 놓이므로 **바 높이·아이콘 형태 이중 신호 필수** |
| tech-artist (M3 추가, 2026-07-27) | 임프 순간이동용 **알파 페이드 셰이더**(소멸/등장) — 3-3절 "초단시간 상태의 이펙트 대체" 판정의 전제. Nearest·픽셀 스냅 기존 설정 불변 |
| gameplay-dev (M3 추가, 2026-07-27) | 신규 적 프레임(숲거미 **28x24**, 무법자 **28x36**(2026-07-30 개정), 임프 **24x28**)에 맞춘 콜리전·발밑 앵커·y-정렬 설정. **인간형 적은 플레이어와 프레임이 동일하므로 플레이어 콜리전 설정을 그대로 재사용 가능.** 궁수 `_rollshot`은 이동 방향과 사격 방향이 독립인데 시트는 **사격 방향 3행 기준**이므로 구르는 방향은 vfx 잔상이 보조하는 구조로 구현 협의(`m3-character-art-plan.md` 6장). 전사 `_spin`은 1행 시트(방향 무관)이므로 방향 행 인덱스 0 고정 |
| **gameplay-dev (2026-07-30 캔버스 개정)** | **인간형 프레임 폭이 20 → 28px로 개정됐다. 진행 중인 `player.tscn` 스프라이트 배선은 그대로 진행·커밋하라** — 파일명·애니메이션 키·프레임 수·프레임 높이(36)·발밑 앵커가 모두 불변이고, `AnimatedSprite2D.offset`이 수평 0(텍스처 중앙 정렬)이라 **W 변경으로 offset·정렬·콜리전을 고칠 필요가 없다**(7-1절 실측). 순서는 **배선 커밋 → pixel-artist 재생성**이며, 재생성 후 확인할 것은 "애니메이션이 그대로 재생되는가" 1건뿐이다. 만약 배선에서 프레임 폭 20을 코드·씬에 **상수로 적어 넣은 곳이 있으면 그 지점만 시트에서 읽도록 바꿔 달라**(하드코딩 금지) |
| gameplay-dev | 프레임 상향(인간형 20x36, 몬스터 20x20/36x28/36x36)에 맞춰 **콜리전 박스·발밑 앵커·y-정렬 오프셋 재튜닝**(1-2절). 콜리전은 하단 발밑 기준 유지 — 프레임 폭이 늘어도 발밑 박스 크기·앵커는 기존 발밑 기준으로 재조정. combat.md 타일=px 환산·`project.godot` 줌/해상도는 불변이므로 이동·전투 수치 코드 변경 없음 |

## 9. 디렉터 결정 필요

- 없음 — 본 문서는 확정된 컨셉(픽셀아트 2D, 밝음6:어둠4, 1280x720) 범위 내의 기준 확정이다.
- 단, **픽셀 스케일(16px 타일, x4 줌)과 팔레트(EDG32)는 게임의 최종 룩을 결정하는 요소**이므로, M2 수직 슬라이스 게이트에서 디렉터가 실제 플레이 화면으로 확인 후 이상 시 재상정한다 (이후 변경은 에셋 재작업 비용이 크므로 M2가 마지막 변경 기회).
- 2026-07-18 갱신: G2-2 플레이 피드백("스타듀밸리에 맞춰라")에 따라 위 재상정 절차가 1회 실행됐다. 검증 결과 타일·줌 구조는 이미 스타듀와 동일했고(1-2-1절), 실제 원인은 인간형 스프라이트 비율이었다 — 이번 개정으로 해당 재상정은 종료되었다고 판단하나, 새 규격(16x32 등)으로 재제작된 실제 플레이 화면은 다음 디렉터 확인 시점(M2 재플레이)에서 한 번 더 검수받는 것을 권장한다.
- **2026-07-27 갱신(M3 아트 태스크)**: 본 개정으로 처리된 판정·확인 사항은 아래와 같다. 상세는 `m3-character-art-plan.md` 12장.
  - **D3-A2-2(붉은 강조색 예외) — art-director 판정 완료**(6-1-1절). `m3-warrior-tier2-skills.md` 12장이 art-director에 위임한 사안이라 **디렉터 결정 필요 항목이 아니다**.
  - **신규 적 프레임 규격·프레임 예산·라이선스 등급제 — art-director 판정 완료**(1-2·1-2-3·3-3·7-1-1절). 확정 컨셉 범위 내의 기준 확정이다.
  - **디렉터 확인 필요 4건**(전부 `m3-character-art-plan.md` 12장에 등재): ① **적 아트 정식화 시점** — 신규 적 7종은 M3에서 "실루엣·상태 식별 수준"으로 두고 손그림 정식화는 M4로 묶을 것을 권고(M3-D3 확정 범위·G3-4 판정 범위의 확인) ② **노상강도·밀렵꾼 M4 이월 승인**(배치 밴드 Lv20·Lv36이 M3 검증 구간 밖) ③ **원본 ZIP 1회 수동 저장 협조**(itch.io 자동 다운로드 실패 시) ④ **라이선스 정책 확인** — 7-1-1 등급제는 비배포 전제이며 배포 검토 시 B·C등급 자산 전면 재검토 필요.
  - **G3-4 판정 항목 추가 요청**: 손그림 품질 상승으로 1-2-2절 "질감 불일치"(16px 청키 타일 위 고밀도 캐릭터) 리스크가 커지므로, 2직업 정식화 완료 직후 실제 플레이 화면에서 1회 확인할 것을 권고한다 — 과하면 캐릭터 명암을 3.5단 수준으로 절제(정상 튜닝 범위).
- **2026-07-30 갱신(M3 3-A 검수·판정)**: 본 개정으로 처리된 판정은 아래와 같다. 상세 근거·실측표는 `m3-character-art-plan.md` 14장.
  - **인간형 프레임 폭 예외 요청 — art-director 판정 완료**(1-2·1-2-3·7-1절). "공격·시전만 28px" 부분 예외는 **기각**하고 **전 상태 28x36 통일**로 개정했다. **디렉터 결정 필요 항목이 아니다** — 실체(척도) 17~19x33과 화면 점유율이 불변이라 **화면에서 캐릭터 크기가 달라지지 않으며**, G2-2("캐릭터가 뚱뚱해 보인다")의 대상이었던 실체 비율에는 손대지 않았다. 캔버스는 투명 여백이다.
  - **전사가 판금 기사보다 경장으로 보인다는 지적 — 수용(현행 방향 유지)**. `jobs.md` 2장은 판금·방패 정체성을 **기사 계열**에 배정했고 전사는 "대검·느리지만 무거운 한 방", 2차는 **검투사**(경장 + 대형 무기)다. 따라서 경장 실루엣은 정합이며, 실제 결함은 **무기가 한손검 규격이라 대검으로 안 읽히는 것**이다 — 8장 pixel-artist ③④로 처방했다. 방어구 계열 교체(판금 다리·투구 추가)는 하지 않는다.
  - **얼굴 표정 부재 — 수용·상한 규정화**(3-2-2절). 표정은 초상화 64x64와 상태 UI 담당으로 이관.
  - **디렉터 확인 필요 1건(신규)**: 재생성 후 실제 플레이 화면에서 **대검 실루엣이 "무거운 한 방"으로 읽히는지** 1회 확인(G3-4 판정 항목에 통합 요청). 규격 판단이 아니라 손맛·정체성 체감 확인이다.
- **2026-07-27 갱신(스톤샤드식 안 1 승인 반영)**: 디렉터가 샘플(전사 20×36·~3.3등신·명암 4단)을 확인하고 "16px 타일 유지, 캐릭터·몬스터만 스톤샤드식 상향"(제안서 안 1)을 확정했다. 본 개정으로 인간형 20x36·잡몹 +4/+4 상향·캐릭터 명암 4단·G2-2 부분 되돌림(1타일→1.25타일 폭)이 모두 디렉터 승인 사항으로 반영됐다(변경 이력·1-2·1-2-2·3-2-1절). **남은 확인 1건**: 16px 청키 타일 위 고밀도 캐릭터의 질감 불일치 여부(1-2-2절 리스크)는 M2 수직 슬라이스 실제 플레이 화면에서 디렉터가 눈으로 확인 — 과하면 캐릭터 명암을 3.5단 수준으로 절제(정상 튜닝 범위, 방향 재결정 아님).
